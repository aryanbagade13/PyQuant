# Examples

- `pricing_and_greeks.py` demonstrates the core pricing library.
- `volatility_surface.py` builds and plots a live volatility surface.
- `live_data/` contains provider smoke checks that require credentials and
  network access.

These files are demonstrations, not automated tests. The supported dataset
workflow is exposed through the `pyquant-earnings` command.
